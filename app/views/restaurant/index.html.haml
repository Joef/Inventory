%h1 Restaurant Manager

.row-fluid
  .span8
    %h3.inline Vendors
    [
    = link_to 'Add New', new_vendor_path
    ]
    .clear
    %table.table.table-striped
      %tr
        %th= sortable "name"
        %th Last Invoice
         
        %th Amount
        %th Date
        %th
        %th= "Account in #{Time.now.year - 1}"
        
    
      - @vendors.each do |vendor|
        = render vendor
    

    %br
    
  .span4
    %h3 Recipes
    %ul
      - @recipes.each do |recipe|
        %li= link_to recipe.title, recipe
        