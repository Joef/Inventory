<%= form_for(@recipe_menu_item) do |f| %>
  <% if @recipe_menu_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_menu_item.errors.count, "error") %> prohibited this recipe_menu_item from being saved:</h2>

      <ul>
      <% @recipe_menu_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.number_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :menu_item_id %><br>
    <%= f.number_field :menu_item_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
